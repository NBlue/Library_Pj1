Nguyên tắc thêm sách:
 Các dữ liệu input thường sẽ lưu trong req.body
 Img sẽ tải uploadfile express để lấy ra img và lưu trong foder của projective và trong server  
 Tác giả: lấy value và dùng router POST: /add/:id để phía controller nhận được giá trị req.params.id và lấy được id tác giả lưu vào server

     /*$(document).ready(function() {
        $("#book-man").on("click", function(event) {
            event.preventDefault();
            const data = $(this).serialize();
            console.log(data);
            $.ajax({
                type: "GET",
                url: "/admin/books",
                data: data,
                success: function(result){
                    console.log(result);
                    {{!-- let html = res.toString().slice(res.toString().indexOf('<div class="container-admin">'), res.toString().indexOf('<footer class="footer">'))
                    console.log(html); --}}

                    $(".container > .grid").html(`<p>Xin chao cac ban<p>`)
                    console.log("Successfull");
                },
                error: function(){

                }    
            })
        })
    }) */